{% extends 'base.html.twig' %}

{% block body %}
        <a href="{{path('listePromos')}}">Afficher toutes les promotions</a>
        <h1>Modification de la fiche d'une promotion</h1>
        {{ form_start(formModif) }}
        <div style="width:200px">
            {{ form_row(formModif.anneePromo) }}
        </div>
        <div style="width:200px">
            {{ form_row(formModif.themeEuropePromo) }}
        </div>
        <div style="width:200px">
            {{ form_row(formModif.cheminDSPPromo) }}
        </div>
        {{ form_row(formModif.submit) }}
        
        {{ form_end(formModif) }}

{% endblock %}